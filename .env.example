# Required: any chat model is reached through OpenRouter by slug.
OPENROUTER_API_KEY=

# Optional: enables higher-quality web search. Without it the agent
# falls back to a free, best-effort DuckDuckGo scrape.
TAVILY_API_KEY=

# Optional tuning.
AGENT_MAX_SEARCHES=5
AGENT_SEARCH_TIMEOUT_MS=9000
